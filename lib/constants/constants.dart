import 'package:flutter/material.dart';

const tDefaultPadding = 20.0;

const PrimaryColor = Color(0xFF055aa0);
const ContainerColor = Color(0xFF0077b5);
const Orange = Color(0xFFFA662E);
const Red = Color(0xFFFF0000);
const White = Color(0XFFffffff);
const Green = Color(0xFF25B701);
const Gray = Color(0XFF757575);
const LightGray = Color(0xFFf0f0f0);
const Black = Color(0XFF000000);
const Blue = const Color.fromRGBO(66, 165, 245, 1);

const tBoxShadow = BoxShadow(
  color: Color(0x28000000),
  blurRadius: 4,
  offset: Offset(0, 2),
);

const LinearGradient tGradient = LinearGradient(
  colors: [
    Color(0xFF9bdaff),
    Color(0xFFfafeff),
  ],
  stops: [0.0, 1.0],
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);
